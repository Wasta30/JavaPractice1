package day51_Map;

public enum Browser {


    CHROME,
    FIREFOX,
    OPERA,
    SAFARI,
    EDGE










}

/*
    // u can use enum for special data type
    // for ex String browwser = "cydeo"
    // so instead of this i gave Borwser browser = "Chrome";
    // insated of string i create enum which is a Browser
    // so enum will only accept one of those constant which are declred while creating enum
    /// for ex week days
     */
// uppercase letter are prefered for consrant
