package day45_Interface_AbstractionReview;

public class InterfaceIntro {

// abstraction : hiding implenattion deatils
  //  instantiated means : represent
    // abstrat class not be declared final
    // interface : provide additonal method
    // we shoukd noy worry about for small detail in interface
    // u can not create any instance / object from interface
    // the variable in interface can only be static or final and not be instance
    // interface can not have a constructor

    // in interface u dont need abstract keyword
    // we can reach 100 percent abstractiohnn by interface and there must be innheritance
    // so the modifier is public onnly inn innterface
    // and its given by default





}
